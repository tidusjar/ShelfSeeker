# IRCBooks Environment Configuration
# Copy this file to .env and customize as needed

# ============================================
# Application Paths
# ============================================

# Directory where downloaded ebooks will be stored
# Default: ./downloads
DOWNLOAD_PATH=./downloads

# Directory where application data and config will be stored
# Default: ./data
DATA_PATH=./data

# ============================================
# Application Settings
# ============================================

# Application port (for both web UI and API)
# Default: 3001
PORT=3001

# Node environment (production/development)
# Default: production (in Docker)
NODE_ENV=production

# ============================================
# Note: IRC Configuration
# ============================================
# IRC settings (server, port, channel, search command) are now
# configured entirely through the web UI and stored in config.json.
# They are no longer set via environment variables.
